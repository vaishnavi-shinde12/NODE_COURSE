const greeting = "Hello World";  // JS uses double quote to store string
const answer = 'Forty Four';     // JS uses single quote to store string

const statement = `the value of PI is ${Math.PI.toFixed(2)}`;      // modern JS uses backticks
console.log(statement);             // we can use interpolation for modern ES6

const msg = "I just love it";
console.log(`Coffee is great because ${msg} `);


const address = `flat no 4
yeola
nashik`;
console.log(address);